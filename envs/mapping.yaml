#mapping.yaml
name: ssuitslsu-mapping
channels:
  - conda-forge
  - bioconda
  - defaults
channel_priority: strict

dependencies:
  - python=3.9
  - bwa-mem2
  - samtools=1.17
  - bc
  - pip
  - pip:
    - pysam==0.21.0
